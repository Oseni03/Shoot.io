---
labels: implemented
---

> _This was generated by AI during triage._

## Context

Issue 001 is complete — `packages/shared/` exists with `AuthService`, `TokenStore`, Zod schemas, `snakeCaseSchema()`, `extractApiError()`, and shared constants (`API_ENDPOINTS`, `STORAGE_KEYS`, `PROJECT`). The extension already imports from `shared`.

The web app (`apps/web/`) has **inline copies** of all the above. It needs to be migrated to import from `shared`, proving the shared interface works for both clients.

## What to build

Replace inline code in `apps/web/` with imports from `shared`. No user-facing changes. Migration map:

| Inline file | Import from `shared` |
|---|---|
| `src/lib/auth/auth-service.ts` | `AuthService`, `SignupResponse` |
| `src/lib/auth/types.ts` | `TokenStore`, `CallOptions` |
| `src/lib/auth/errors.ts` | `extractApiError`, `AuthError`, `AuthNetworkError`, `AuthValidationError`, `AuthSessionError` |
| `src/lib/utils.ts` (only `snakeCaseSchema` portion) | `snakeCaseSchema` |
| `src/schemas/` (all schemas) | re-exported via `export * from "shared"` |
| `src/lib/config.ts` (only `API_ENDPOINTS`, `STORAGE_KEYS`, `PROJECT` portions) | respective exports |

What stays web-specific:
- `src/lib/config.ts`: `ENV`, `ROUTES`, `DEFAULTS`, `NAV_LINKS`, `QUERY_KEYS`
- `src/lib/api.ts`: Axios instance, request/response interceptors, web's `tokenStore` (localStorage + cookie)
- Only the `snakeCaseSchema` re-export in `src/lib/utils.ts` — the rest of `cn()` etc. stays

## Constraints

- **Axios adapter required.** The shared `AuthService` constructor is `(httpClient: HttpClient, baseUrl: string, tokenStore: TokenStore)` — it uses a generic `fetch`-compatible `HttpClient`. The web's current constructor is `(api: AxiosInstance, tokenStore: TokenStore)`. Build a thin adapter in `apps/web/src/lib/api.ts` that wraps the Axios instance as an `HttpClient`. The adapter must preserve existing request/response interceptors (token injection, 401 redirect).
- Singleton construction in `auth-service.ts` line 205 (`export const authService = new AuthService(api, tokenStore)`) must be updated to pass `baseUrl` (from `NEXT_PUBLIC_API_URL`) and the adapter.
- Add `"shared": "*"` to `apps/web/package.json` dependencies.
- Run `npm install` at root to link the workspace.
- Remove each inline file only after all imports in the codebase have been redirected.
- Do **not** touch `apps/extension/` or `packages/shared/` code.
- ADR 0001 (Decision 3) established the shared workspace boundary — no React/Next.js/window deps in shared code.

## Acceptance criteria

- [ ] `npx tsc --noEmit` passes for `apps/web/` (TypeScript type-check)
- [ ] `npm run lint` passes for `apps/web/` (Biome check)
- [ ] Login, register, password reset, and all authenticated pages in the web app work identically to before (manual smoke test)
- [ ] No remaining imports from the old inline locations (verify with `grep` — no files in `apps/web/src/` should import from `@/lib/auth/auth-service`, `@/lib/auth/types`, `@/lib/auth/errors`, `@/schemas`, or the inline portions of `@/lib/config`)

## Rollback plan

`git revert` the commit — this is a pure import-path refactoring with no database, no migrations, no billing. If TypeScript compilation fails, the change is caught before deploy. No feature flags needed.

## Risk

`Normal` — no database, no billing, no migrations. Parallel agents are safe on other slices.

## Out of scope

- No changes to `apps/extension/` or `packages/shared/`
- No changes to `apps/web/src/lib/api.ts` beyond adding the `HttpClient` adapter
- No behavioral changes to any auth, API, or schema logic
- No test framework installation or test writing for the web app

## Parent

[PRD: Chrome Extension Boilerplate](../prd/chrome-extension-boilerplate.md)
